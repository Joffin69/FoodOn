// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCyUWVMfzy53Ax7gDKTEbGSSb7oaLT49mk",
    authDomain: "minfy-foodon.firebaseapp.com",
    databaseURL: "https://minfy-foodon.firebaseio.com",
    projectId: "minfy-foodon",
    storageBucket: "minfy-foodon.appspot.com",
    messagingSenderId: "814233222529",
    appId: "1:814233222529:web:f90031ad7a9e0560953c80",
    measurementId: "G-ZLCQF14533"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
